apiVersion: v1
kind: Service
metadata:
  name: {{ include "cd-helm.fullname" . }}
  labels:
    {{- include "cd-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cd-helm.selectorLabels" . | nindent 4 }}
